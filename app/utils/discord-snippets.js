/**
 * @fileoverview A collection of useful discord snippets.
 *
 *  --- This module is not required by anything. ---
 */

// Send the message to a designated channel on a server:
// const channel = member.guild.channels.cache.find(
//   (ch) => ch.name === 'bot-test',
// );
